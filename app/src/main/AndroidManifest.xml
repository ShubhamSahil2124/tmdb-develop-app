<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.movies.tmdb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_tmdb_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_tmdb_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="com.movies.tmdb.ui.SplashScreenActivity"
                    android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.movies.tmdb.ui.TvDetailsActivity" />
        <activity
            android:name="com.movies.tmdb.ui.TvShowActivity"
            android:label="@string/tv_show" />
        <activity
            android:name="com.movies.tmdb.ui.MovieDetailsActivity"
            android:label="@string/Movie_Detail" />
        <activity
            android:name="com.movies.tmdb.ui.MoviesActivity"
            android:label="@string/movies" />

        <activity
            android:name="com.movies.tmdb.ui.MainActivity"
            android:label="@string/app_name"/>
    </application>

</manifest>